#include<iostream>
using namespace std;
int main(){
    // int a,b,sum=0;
    // cin>>a>>b;
    // while (a!=0){
    //     if (a%10==b){sum++;}
    //     a=a/10;
    // }
    // cout<<sum;

    /*int k,sum=0,ci=0;
    cin>>k;
    for (int c=1;c<=k;c++)   //   c<=k    操作较迷，无用处，ci早就达到跳出循环输出条件，正常来说根本就不存在跳出for循环的情况
        {for (int a=1;a<=c;a++){
            sum+=c;
            ci++;
            if(ci==k){cout<<sum;return 0;}
        }}*/

    // int k,sum=0,ci=1,b=1;
    // cin>>k;
    // bool f=true;
    // while(f==true){
    //     for (int a=1;a<=b;a++){
    //         sum+=b;
    //         if(ci==k){f=false;break;}
    //         ci++;}
    //     b++;}
    // cout<<sum;

    // long long b=0;
    // for (int c=1;c<=3;c++){long long a=1;
    //     for (int d=1;d<=c;d++){a=a*d;}
    //     b+=a;
    // }
    // cout<<b;

    // int a,sum=0;
    // cin>>a;
    // for (int b=1;b<=a;++b){int c,d;cin>>c>>d;if (c==d){sum++;}}
    // cout<<sum;

    // int a,d=1;
    // cin>>a;
    // for (int b=1;b<=a;++b){
    //     for (int c=1;c<=b;++c){cout<<d<<" ";d++;}
    //     cout<<endl;
    // }
    return 0;
}