#include<iostream>
#include <cmath>
//包含fabs,sqrt,pow,exp(以e为底),log(自然对数),ceil(向上取整),floor(向下取整)
using namespace std;
int main(){
    cout<<10/3;     // c++中用/只能得到整数
    cout<<10.0/3<<endl;     // c++中用/只能得到整数,但算式中含有浮点数即可
    // cout<<sqrt(36)<<"m\n";
    // cout<<pow(5,2)<<"m2";
    cout<<pow((34+23),2);
    return 0;
}

